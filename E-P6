#include <stdio.h>

int main() {
    int N, i;
    long long fact = 1;

    printf("Enter a number: ");
    scanf("%d", &N);

    for(i = 1; i <= N; i++) {
        fact = fact * i;
    }

    printf("Factorial = %lld", fact);

    return 0;
}